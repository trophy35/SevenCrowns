using System;

namespace SevenCrowns.Map.Cities
{
    /// <summary>
    /// City tier used to determine daily gold yield.
    /// </summary>
    public enum CityLevel
    {
        Village = 0,
        City = 1,
        Fortress = 2,
        Capital = 3
    }
}

